Your current design is already clean. The black & white monochrome theme is actually the right call — it looks premium and timeless. Here's your Replit prompt:

---

**Copy and paste this entire prompt into Replit:**

---

Redesign the entire Truvornex platform UI with a world-class monochrome design system. No colors at all — only black, white, and greys. This should look better than what Anthropic or Google's design team would ship. Here are the exact rules:

**THEME — MONOCHROME ONLY**
- Dark mode background: `#0a0a0a` (near black, not pure black)
- Dark mode surface cards: `#111111`
- Dark mode elevated cards: `#1a1a1a`
- Dark mode borders: `#2a2a2a`
- Dark mode primary text: `#f5f5f5`
- Dark mode muted text: `#888888`
- Light mode background: `#fafafa`
- Light mode surface cards: `#ffffff`
- Light mode elevated: `#f0f0f0`
- Light mode borders: `#e0e0e0`
- Light mode primary text: `#0a0a0a`
- Light mode muted text: `#666666`
- Accent/interactive elements: pure white on dark, pure black on light
- Zero colors anywhere — no blue buttons, no green badges, nothing

**TYPOGRAPHY**
- Font: Inter or Geist (import from Google Fonts)
- Hero heading: 56px bold, tight letter spacing -0.03em
- Section headings: 24px semibold
- Body: 15px regular, line height 1.6
- Captions/labels: 12px, letter spacing 0.05em uppercase

**LAYOUT**
- Keep the left sidebar navigation
- Sidebar width: 220px, background `#0d0d0d` in dark
- Main content max-width: 1100px, centered
- Generous padding: 32px on desktop
- Cards with `border-radius: 16px`
- Subtle borders `1px solid #1e1e1e` in dark mode

**COMPONENTS**
- Search bar: full-width pill shape, `border-radius: 999px`, subtle border
- Category cards: square aspect ratio, icon centered, label below, hover lifts with `box-shadow`
- Stats row: clean horizontal cards, large bold numbers, small grey labels
- Buttons: primary = white background black text (dark mode), rounded pill shape
- Badges: outline style only, no filled colors
- Sidebar nav items: hover state is white text + left white border indicator

**EFFECTS**
- Hover transitions: `transition: all 0.2s ease`
- Card hover: `transform: translateY(-2px)` + slightly brighter border
- No gradients anywhere except a very subtle `radial-gradient` glow behind the hero text (white, 5% opacity max)
- Glassmorphism only if very subtle: `backdrop-filter: blur(10px)` on floating elements

**DARK/LIGHT TOGGLE**
- Smooth transition between modes: `transition: background 0.3s, color 0.3s`
- Toggle in sidebar bottom as a clean icon button

**OVERALL FEEL**
- Think: Arc browser meets Linear.app meets Apple.com
- Minimal, breathable whitespace
- Every element should feel intentional
- No clutter, no unnecessary decorations
- Professional but human

Apply this design system to every page: Home, Services, Provider pages, Admin dashboard, Chat, and all other screens consistently.

---

That prompt will get you a serious upgrade. Try it and show me the result 🫡